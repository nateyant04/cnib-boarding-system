package com.cnib.dogboarding.entity;

/**
 * Enum defining the four user roles in the system.
 * Each role has different permissions and access levels.
 */
public enum UserRole {
    BOARDER,
    PUPPY_RAISER,
    TRAINER,
    COORDINATOR
}
